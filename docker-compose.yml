version: "3.9"

services:
  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - java-api
      # - fastapi1
      - fastapi2

  java-api:
    build: ./Backend/optimizacionespacios
    ports:
      - "8080:8080"

  # fastapi1:
  #   build: ./apijulian
  #   ports:
  #     - "8000:8000"

  fastapi2:
    build: ./AlgoritmoS
    ports:
      - "8001:8000"
